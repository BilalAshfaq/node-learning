const emps = require('./names');
const sayHello = require('./sayHi');
const {items, car}  = require('./objects');
require('./mind-grenade');
console.log(items);

sayHello(emps.ali);
sayHello(emps.bili);